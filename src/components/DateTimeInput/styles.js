import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    datePlace:{
        fontWeight: 'bold',
    },

    input:{
        color: '#050505',
        fontSize: 16,
        fontWeight: 'bold',
        padding: 10,
        width: '90%',
        borderBottomWidth: 1,
        borderBottomColor: '#9FD996',
        marginHorizontal: 20
    },
});

export default styles;